[package]
name = "{{ crate_name }}_ios"
version.workspace = true
edition.workspace = true

[dependencies]
{{ crate_name }} = { path = "../lib" }

[lib]
name = "{{ crate_name }}"
crate-type = ["staticlib"]
